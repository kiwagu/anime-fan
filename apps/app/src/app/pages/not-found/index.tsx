import React from 'react';

import './index.scss';

/* eslint-disable-next-line */
export interface NotFoundProps {}

export const NotFound = (props: NotFoundProps) => {
  return (
    <div>
      <h1>Welcome to not-found!</h1>
    </div>
  );
};

export default NotFound;
